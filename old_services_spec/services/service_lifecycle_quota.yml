properties:
  service_plans:
    mysql:
      free:
        configuration:
          lifecycle:
            snapshot:
              quota: 0
      "100":
        configuration:
          lifecycle:
            snapshot:
              quota: 1
    postgresql:
      free:
        configuration:
          lifecycle:
            snapshot:
              quota: 0
      "100":
        configuration:
          lifecycle:
            snapshot:
              quota: 1
    mongodb:
      free:
        configuration:
          lifecycle:
            snapshot:
              quota: 0
      "100":
        configuration:
          lifecycle:
            snapshot:
              quota: 1
    redis:
      free:
        configuration:
          lifecycle:
            snapshot:
              quota: 0
      "100":
        configuration:
          lifecycle:
            snapshot:
              quota: 1
